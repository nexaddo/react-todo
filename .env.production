# ===========================
#  Production Environment
# ===========================
NODE_ENV=production

# ---------------------------
#  Server Configuration
# ---------------------------
PORT=3000

# The frontend origin allowed for CORS.
# 🔹 Update this to match your deployed frontend URL.
# Examples:
#   https://quick-todo-client.onrender.com
#   https://your-static-webapp.azurestaticapps.net
CLIENT_ORIGIN=https://react-todo-0hsm.onrender.com

# Path for persistent todo storage (inside container or volume)
DATA_PATH=/app/data/todos.json

# Logging verbosity (options: trace | debug | info | warn | error | silent)
LOG_LEVEL=info

# ---------------------------
#  Client Build Variables (Vite)
# ---------------------------
# These can be used during build if you copy this file into client/.env.production
VITE_API_BASE=https://example-backend-url.com/api
VITE_APP_NAME=Quick Todo
VITE_ENV=production

# ---------------------------
#  Security / Optional
# ---------------------------
# Force HTTPS redirect in production if behind proxy (e.g. Azure, Render)
ENFORCE_HTTPS=true
# JWT_SECRET=super_secret_key_for_signing_tokens

# ---------------------------
#  Platform Notes
# ---------------------------
# 🔹 Render
#   - Both CLIENT_ORIGIN and VITE_API_BASE must match deployed endpoints.
#   - Add vars via Render Dashboard > Service > Environment.
#
# 🔹 Azure
#   - Add via App Service > Configuration or via workflow secrets.
#   - Ensure WEBSITE_NODE_DEFAULT_VERSION=20.
#
# 🔹 Docker
#   - Place this file in project root and reference it from compose.yaml:
#       env_file:
#         - ./.env.production
